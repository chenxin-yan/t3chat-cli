# t3chat-cli

A CLI built with [Crust](https://crustjs.com).

## Development

```sh
# Run in dev mode
bun run dev

# Type-check
bun run check:types

# Build standalone executable
bun run build
```

## Usage

```sh
# Run the CLI
t3chat-cli world
t3chat-cli --greet Hey world
```
